name: Shell Commands

on: [push] # pull

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello Github Action!"
      - name: multiline script
        run: |
          node -v
          npm -v
          